# winterm-web runtime dependencies (keep this file ASCII-only:
# pip reads requirements.txt with the locale encoding, not UTF-8).
#
# anyio must be >= 4.9. 4.6.2 + starlette 1.0 crashes in async route body
# parsing (MemoryObjectItemReceiver AttributeError). See docs/DEVLOG.md.
fastapi>=0.115
uvicorn>=0.30
websockets>=13
anyio>=4.9
python-multipart>=0.0.9

# The PTY binding - exactly one of these installs, picked by the platform marker.
# Windows uses ConPTY via pywinpty; macOS/Linux use a forked pty via ptyprocess.
# Both are wrapped by pty_backend.py, which is the only module that imports either one.
pywinpty>=2.0 ; sys_platform == "win32"
ptyprocess>=0.7 ; sys_platform != "win32"
