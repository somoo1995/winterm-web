# ⚠ PowerShell 5.1 은 BOM 없는 UTF-8 을 ANSI 로 읽는다(한글이 깨진다).
#   .ps1 은 UTF-8 BOM + CRLF 를 유지해야 한다 — 줄바꿈을 git 이 바꾸지 못하게 고정한다.
*.ps1 text eol=crlf

# 바이너리 — diff/줄바꿈 변환 대상이 아니다
*.png   binary
*.ico   binary
*.woff2 binary
*.woff  binary
*.ttf   binary
*.exe   binary
